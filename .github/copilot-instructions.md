# Copilot Instructions

Use the repo `AGENTS.md` files as the main source of agent guidance.

- Run `make test` before finishing engine changes.
- Run `node --check` on changed web JavaScript modules.
- Keep adjudication pure and data-driven.
- Preserve the province/location split in the engine.
- Keep the web prototype dependency-free unless explicitly asked otherwise.
